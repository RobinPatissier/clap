{% extends 'base.html.twig' %}

{% block body %}
    <h1>Post a New Pop in {{ group.name }}</h1>

    {{ form_start(form) }}
        {{ form_row(form.content) }}
        <button class="btn btn-primary">Post</button>
    {{ form_end(form) }}
{% endblock %}
